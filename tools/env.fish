if not contains "/projects/p57098/euijin1/Caver/tools" $PATH
    # Prepending path in case a system-installed binary needs to be overridden
    set -x PATH "/projects/p57098/euijin1/Caver/tools" $PATH
end
